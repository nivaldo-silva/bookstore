server:
  port: 8080 

spring:
  application:
    name: bookstore 
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver 
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true 
    properties:
      hibernate:
        '[format_sql]': true isso.
  sql: 
    init:
      mode: always 